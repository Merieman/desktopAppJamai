<Window x:Class="graph1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="CSV Searcher" Height="504" Width="815" WindowState="Maximized">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10">
            <RadioButton x:Name="rbValue" Content="Search by value" Margin="10"/>
            <RadioButton x:Name="rbDate" Content="Search by date" Margin="10"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="10">
            <TextBox x:Name="tbSearch" Margin="10" Width="200"/>
            <Button x:Name="btnSearch" Content="Search" Margin="10" Click="btnSearch_Click" IsEnabled="False" />
        </StackPanel>

        <DataGrid x:Name="dgResults" Grid.Row="2" Margin="10,10,745,10" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Numero de charge" Binding="{Binding Ncharge}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
